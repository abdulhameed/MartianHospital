from django.db import models

# Appointment model

# class Appointment(models.Model):
#     staff =
#     patient =
#     pendin_approval = models.BooleanField(default=True, null=True)
#     approved = models.BooleanField(default=False, null=True)
